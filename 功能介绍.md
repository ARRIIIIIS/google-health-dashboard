# 健康仪表盘小组件 · 功能介绍

一个常驻 macOS 桌面的健康监控卡片（Übersicht widget），数据来自 Google Health API（Fitbit / Health Connect），把步数、心率、睡眠、血氧等关键指标聚合到一屏，并带一个会"表达情绪"的球和一个久坐提醒。

---

## 一、数据从哪来

- 后端采集脚本 `fetch_standalone.py` 直连 Google Health API（v4），每轮拉取当天全部指标，写入 `data.js`。
- 小组件（Übersicht）按 `refreshFrequency = 300000`（**每 5 分钟**）自动重跑采集命令；也可手动点「刷新」立即触发。
- 情绪球、久坐弹窗由本地常驻服务驱动：`emotion-ball`（:9283）、`sed_reset_server`（:9293）。

---

## 二、哪些是「实时」、哪些是「定时刷新」（重点）

所有**健康数据都是定时刷新**的——最快 5 分钟一次，或你点「刷新」才更新。
小组件里真正"实时 / 连续"的，是前端动画和点击即时反馈。

| 数据 / 元素 | 类型 | 说明 |
|---|---|---|
| 步数、距离、卡路里 | 定时刷新 | 每 5 分钟（或点刷新）从 API 拉取当日累计 |
| 活跃分钟（AZM） | 定时刷新 | 同上 |
| **心率** | 定时刷新（但取"最新采样"） | 显示设备**最近一次**心率采样值，不是日均/静息均值；但刷新周期同样是 5 分钟。属于"实时读数语义 + 定时刷新"——你刚运动完，要等下一轮刷新才体现 |
| 静息心率、HRV、呼吸率、血氧 | 定时刷新 | 每日汇总指标，每 5 分钟更新 |
| 睡眠（深睡 / REM / 浅睡 / 清醒） | 定时刷新 | 取昨夜睡眠记录，每天基本不变 |
| 久坐计时 `idle_min` / 是否久坐 | 定时刷新 | 由后端 `check_sedentary()` 计算后写入，每 5 分钟刷新 |
| 健康提示（AI 洞察） | 定时刷新 | 结合今日 vs 昨日生成，每 5 分钟更新 |
| **情绪球动效**（呼吸 / 眨眼 / 视线 / 每 ~4.5s 自动换表情） | **实时连续** | 纯前端动画，不依赖数据刷新，持续运行 |
| **久坐弹窗**脉冲呼吸、弹出动画 | **实时连续** | CSS 动画，持续运行 |
| **点「起来了」** | **实时交互** | 点下立即 `fetch(:9293/reset_sedentary)` 重置数据，并触发 90s 前端强制窗口——立刻显示"0 / 灰色 / 快乐球"，不用等 5 分钟 |
| **点「刷新」** | **实时交互** | 立即强制重跑采集，拿到最新数据 |
| **点球换表情** | **实时交互** | 点击情绪球立即切换到下一个表情 |

> 一句话总结：**数据是准实时的（最新快照，5 分钟刷新一次）；动画和点击反馈是真正的实时。**

---

## 三、主要功能模块

1. **步数 Hero + 进度条**
   超大数字显示今日步数，下方进度条对照 8000 步目标；右侧显示距离（km）、卡路里（千卡）和活跃分钟。

2. **四项核心指标**
   心率（bpm）、HRV（ms）、血氧（%）、呼吸（次/分）一行四格平铺，颜色区分、带趋势微光背景。

3. **睡眠阶段条**
   横向分段展示深睡 / REM / 浅睡 / 清醒占比，并显示总睡眠时长。

4. **健康提示（AI 洞察）**
   结合"今日 vs 昨日"对比 + 时段上下文，生成一句自然语言建议（如"今天比昨天活跃""HRV 下降，可能疲劳"）；出现血氧偏低、睡眠严重不足等红线时升级为告警。

5. **情绪球**
   一只 blob 造型的球，根据健康状态切换表情：日常在 20 个轻松表情里循环；深夜切睡眠表情；**只有久坐提醒时才随机变愤怒 / 出错**；提示为 alert 级时变"慌张"。

6. **久坐提醒**
   - 顶部"静坐计时"胶囊：正常灰色，久坐时变琥珀色并呼吸闪烁。
   - 球正下方弹出提醒气泡：显示已静坐分钟数，提示"起来走 3 分钟即可重置"。
   - 「稍后」：暂时收起，稍后再评估。
   - 「起来了」：立即重置计时（前端即时反馈 + 后端复位），下一轮刷新自然归零。
   - 已彻底关闭 macOS 系统通知横幅，只在小组件内提醒。

---

## 四、交互一览

- **刷新图标**（右上角 ↻）：立即重新采集全部数据。
- **点击情绪球**：手动切换到下一个表情。
- **点击静坐胶囊**（仅久坐时）：开 / 关提醒气泡。
- **「起来了」按钮**：即时重置久坐计时。

---

## 五、刷新机制

- 自动：`refreshFrequency = 300000`（5 分钟）重跑 `fetch_standalone.py` → 写 `data.js` → 渲染。
- 手动：点「刷新」按钮强制重跑。
- 防清零：若某一轮核心指标全空（疑似 API 瞬时失败），保留上次有效数据，不覆盖。
- Token 过期自动用 refresh_token 续期；续期失败则跳过本轮、保留旧数据。

---

## 六、备注与边界

- **心率是"最新采样"不是"持续流式"**：设备每几秒上报一次，但小组件只在刷新时取最新一条，两次刷新之间数值不变。
- 睡眠取**昨夜**记录，白天基本稳定；上午侧重睡眠/活跃提示，下午改为趋势对比。
- 另有浏览器独立版（:8911，由 `standalone-server.js` 提供），与本桌面小组件共用同一套采集脚本，数据一致。
- 依赖本地常驻服务：emotion-ball（:9283）、sed-reset（:9293），需保持运行。
